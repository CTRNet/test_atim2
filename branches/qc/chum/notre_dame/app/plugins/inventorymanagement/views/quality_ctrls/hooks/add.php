<?php 

	// --------------------------------------------------------------------------------
	// Generate default run id
	// -------------------------------------------------------------------------------- 
	if(isset($default_run_id)) {
		$final_options['override']['QualityCtrl.run_id'] = $default_run_id;
	}
	
?>
